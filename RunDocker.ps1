docker compose up -d

# docker compose run --rm frontend sh -c "npm create vite@latest . -- --template react-ts && npm install"

# docker compose run --rm backend sh -c "npm init -y && npm install express socket.io better-sqlite3 cors && npm install -D typescript @types/node @types/express ts-node"

# git add .
# git commit -m "フロントエンドとバックエンドの初期化"
# git push